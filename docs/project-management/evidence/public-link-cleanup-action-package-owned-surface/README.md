# Public Link Cleanup Action Package-Owned Surface

Evidence for `public-link-cleanup-action-package-owned-surface-slice`.

This slice moves local-testing dry-run public link cleanup preview composition
to `larena/link`: cleanup request fixtures, candidate set snapshot, would-clean
snapshot, rollback replay plan, negative guards, final report assembly and
evidence writing.

The entry app keeps only existing internal command/provider/test wiring to the
package preview helper. It does not add production cleanup, scheduler, queue,
persistence, delivery, file deletion or release-ready behavior.
