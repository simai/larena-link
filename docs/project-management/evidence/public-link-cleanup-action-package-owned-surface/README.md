# Public Link Cleanup Action Package-Owned Surface

Evidence for `public-link-cleanup-action-package-owned-surface-slice`.

This slice moves local-testing dry-run public link cleanup preview report
assembly to `larena/link` while preserving the entry-app internal
route/controller/view wire. It does not add production cleanup, scheduler,
queue, persistence, delivery, file deletion or release-ready behavior.
