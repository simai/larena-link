# Tests

Passed.

Required package and root checks:

- package `composer run quality:gate`
- root `php artisan test --filter=PublicLinkGuardedRealDeliveryAdapterFoundationTest`
- root cockpit/debt/ownership/architecture validators
