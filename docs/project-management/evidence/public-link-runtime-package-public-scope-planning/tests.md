# Tests

- `composer run quality:gate`
  - validates metadata sync between `module.yaml` and `.larena/launch-context.json`
  - runs `PublicLinkRuntimePackagePublicScopePlanTest`
  - keeps package evidence and scope contracts valid
