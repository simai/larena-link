# Public Link Runtime Package Public Scope Planning

This evidence package records the exact package-local public route/controller
scope that `larena/link` will need before `public_link_runtime_routes` can move
beyond deferred readiness.

This batch is planning-only. It does not add `routes/public.php`, does not add
`src/Http/Controllers/Public/*` and does not change provider wiring.
