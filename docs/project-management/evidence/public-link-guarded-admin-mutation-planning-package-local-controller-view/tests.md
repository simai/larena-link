# Tests

Package test coverage:

- `tests/Feature/PublicLinkGuardedAdminMutationPlanningRouteBoundaryTest.php`
- `tests/Unit/PublicLinkGuardedAdminMutationPlanningPreviewTest.php`

Root test coverage:

- `tests/Feature/PublicLinkGuardedAdminMutationPlanningFoundationTest.php`

The root test must prove that the route name
`larena.internal.public-link-guarded-admin-mutation-planning` resolves to the
package controller after the root route/controller/view are removed.
