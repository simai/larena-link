# Tests

Expected validation set:

- package PHP syntax checks for the new runtime and unit test;
- package `composer run quality:gate`;
- root feature test `PublicLinkDeliveryContractHardeningFoundationTest`;
- root cockpit, governance, debt inventory, ownership and architecture validators;
- root and package `git diff --check`.

Final pass/fail evidence is recorded in the root implementation report for the batch.

