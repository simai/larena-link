# Public Link Revoke Action Package-Owned Surface

This evidence package records the `public_link_revoke_action` package-owned
read-model/report/composition slice.

The slice moves local-testing revoke preview validation, request fixtures,
before/after snapshots, rollback plan, negative guards and final report
assembly into `larena/link`. The entry app keeps only the existing planning
dependency composition until that upstream planning wrapper is reduced.
