# Public Link Revoke Action Package-Owned Surface

This evidence package records the `public_link_revoke_action` package-owned
read-model/report slice.

The slice moves local-testing revoke preview validation and final report
assembly into `larena/link`. The entry app keeps the planning dependency,
request and before/after/rollback/guard fixtures.
