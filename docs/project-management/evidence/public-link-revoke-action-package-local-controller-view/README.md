# Public Link Revoke Action Package Local Controller View

This evidence bundle records the package-owned presentation slice for the
`public_link_revoke_action` internal developer-preview surface.

The slice only moves the local/testing route, controller and Blade view into
`larena/link`. It preserves the entry-app composed report source and does not
enable production public-link revocation.
