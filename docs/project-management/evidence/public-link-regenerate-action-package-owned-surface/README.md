# Public Link Regenerate Action Package-Owned Surface

Evidence for `public-link-regenerate-action-package-owned-surface-slice`.

This slice moves local-testing public link regenerate preview composition to
`larena/link`: request fixtures, old/new fingerprint snapshots, rollback planning,
negative guards, final report assembly and evidence writing.

The entry app keeps only existing internal command/provider/test wiring to the
package preview helper. It does not add production token generation, storage,
delivery, persistence, queue/scheduler execution or release-ready behavior.
