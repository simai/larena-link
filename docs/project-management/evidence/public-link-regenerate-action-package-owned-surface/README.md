# Public Link Regenerate Action Package-Owned Surface

Evidence for `public-link-regenerate-action-package-owned-surface-slice`.

This slice moves local-testing public link regenerate preview report assembly to
`larena/link` while preserving the entry-app internal route/controller/view wire.
It does not add production token generation, storage, delivery, persistence,
queue/scheduler execution or release-ready behavior.
