# Public Link Regenerate Action Package Local Controller View

This evidence bundle covers the `public_link_regenerate_action` local/testing
internal route, controller and Blade review view migration into `larena/link`.

The slice keeps the entry app as the report source bridge and does not enable
production public-link regeneration, raw regenerated token output, delivery,
storage writes, queue/scheduler runtime or release-ready behavior.
