# Tests

Required:

- `composer run quality:gate`
- `php artisan test --filter=PublicLinkDryRunRuntimePreviewSmokeTest`
- cockpit/debt/ownership validators in the entry app after root bridge removal.
